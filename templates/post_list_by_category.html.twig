{% extends "master.html.twig" %}

{% block title %}post list{% endblock %}

{% block content %}
    <table border="1">
        <tr><th>#</th><th>title</th><th>body</th><th>actions</th></tr>
    {% for p in list %}
            <tr class="{% if loop.index is odd %}rowodd{% else %}roweven{% endif %}">
                <td>{{loop.index}}</td>
                <td>{{p.title}}</td>
                <td>{{p.body}}</td>     
                <td>
                    <a href="/post/delete/{{p.id}}">Delete</a>
                    <a href="/post/edit/{{p.id}}">Edit</a>
                </td></tr>
    {% else %}
        <tr><td colspan="6">You have no posts</td></tr>
    {% endfor %}
    </table>
    
{% endblock %}
